{
  "name": "my-dockerized-app",
  "version": "1.0.0",
  "description": "A sample Node.js application deployed via GitHub Actions and Terraform.",
  "main": "index.js",
  "scripts": {
    "start": "http-server ./dist/html -p 8080",
    "build": "echo 'Building application...' && rm -rf dist && mkdir -p dist && cp src/* dist/ || true && echo 'Build complete: dist directory created.'",
    "test": "jest",
    "lint": "eslint ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1"
  },
  "devDependencies": {
    "eslint": "^8.x.x",
    "eslint-plugin-node": "^11.1.0",
    "jest": "^29.x.x"
  }
}